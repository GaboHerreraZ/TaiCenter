import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.taicenter',
  appName: 'tai-center',
  webDir: 'dist/tai-center',
  bundledWebRuntime: false,
};

export default config;
